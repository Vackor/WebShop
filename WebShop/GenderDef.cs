namespace WebShop
{
    // Gender options class
    class GenderDef
    {
        // name
        private string name;
        public string Name { get; set; }

        // constructor
        public GenderDef(string name)
        {
            Name = name;
        }
    }
}
